/**
 * Delay (in ms) between closing one modal/drawer and opening the next.
 * Allows the close animation to finish before the new one slides in,
 * preventing stacked-drawer issues on mobile.
 */
export const MODAL_TRANSITION_DELAY_MS = 75;
